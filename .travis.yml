language: python

python:
  - 3.7
  - 3.8

before_install:
  - curl -sSL https://raw.githubusercontent.com/python-poetry/poetry/master/get-poetry.py | POETRY_HOME=/etc/poetry python

install: /etc/poetry/bin/poetry install

script: /etc/poetry/bin/poetry run py.test
